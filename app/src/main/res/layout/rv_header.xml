<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    tools:ignore="ContentDescription"

    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/rv_header_logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:src="@drawable/ic_rv_screen_logo"

        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"

        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <ImageView
        android:id="@+id/rv_header_language"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:src="@drawable/ic_lang"

        android:layout_marginEnd="16dp"

        app:layout_constraintTop_toTopOf="@id/rv_header_logo"
        app:layout_constraintBottom_toBottomOf="@id/rv_header_logo"
        app:layout_constraintEnd_toEndOf="parent"/>

    <ImageView
        android:id="@+id/rv_header_mail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:src="@drawable/ic_mail"

        android:layout_marginEnd="32dp"

        app:layout_constraintTop_toTopOf="@id/rv_header_language"
        app:layout_constraintBottom_toBottomOf="@id/rv_header_language"
        app:layout_constraintEnd_toStartOf="@id/rv_header_language"/>

</androidx.constraintlayout.widget.ConstraintLayout>